#include <stdio.h>
#include <stdlib.h>

int main(char src[], char code[])
{

    printf("message original:\n");
    scanf("%s \n",src);
    int i, j;
    while (j)
    for (i=0;i<3;i++)
    return 0;
}
